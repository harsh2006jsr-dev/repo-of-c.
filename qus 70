Rotate an array to the right by k positions.

/*
Sample Test Cases:
Input 1:
5
1 2 3 4 5
2
Output 1:
4 5 1 2 3

*/
#include <stdio.h>
void rightRotate(int arr[], int n, int k) 
{
    k = k % n; // Normalize k
    int temp[n];
    for (int i = 0; i < n; i++) 
    {
        temp[(i + k) % n] = arr[i];
    }
    // Copy temp to arr
    for (int i = 0; i < n; i++) 
    {
        arr[i] = temp[i];
    }
}
int main() 
{
    int arr[] = {1, 2, 3, 4, 5};
    int n = 5, k = 2;
    rightRotate(arr, n, k);
    for (int i = 0; i < n; i++)
        printf("%d ", arr[i]);
    return 0;
}
