Write a program to calculate simple and compound interest for given principal, rate, and time.

#include <stdio.h>
#include <math.h> // Required for the pow() function

int main() 
{
    double principal, rate, time;
    double simple_interest, compound_interest;
    double total_amount_si, total_amount_ci;
    printf("Enter the principal amount: ");
    scanf("%lf", &principal);

    printf("Enter the annual interest rate (e.g., 5 for 5%%): ");
    scanf("%lf", &rate);

    printf("Enter the time period in years: ");
    scanf("%lf", &time);
    simple_interest = (principal * rate * time) / 100.0;
    total_amount_si = principal + simple_interest;
    total_amount_ci = principal * pow((1 + rate / 100.0), time);
    compound_interest = total_amount_ci - principal;
    printf("\n----------------------------------------\n");
    printf("Principal:      %.2lf\n", principal);
    printf("Rate:           %.2lf%%\n", rate);
    printf("Time:           %.2lf years\n", time);
    printf("----------------------------------------\n\n");

    printf("--- Simple Interest Details ---\n");
    printf("Interest Earned:   %.2lf\n", simple_interest);
    printf("Total Amount (A):  %.2lf\n\n", total_amount_si);

    printf("--- Compound Interest Details ---\n");
    printf("Interest Earned:   %.2lf\n", compound_interest);
    printf("Total Amount (A):  %.2lf\n", total_amount_ci);

    return 0;
}