Q118: Write a program to take an input array of size n. The array should contain all the integers between 0 to n except for one. Print that missing number

/*
Sample Test Cases:
Input 1:
nums1 = [0,3,2,4]
Output 1:
1

Input 2:
nums1 = [1,2,3]
Output 2:
0

Input 3:
nums1 = [0,4,3,1,5]
Output 3:
2

*/
#include <stdio.h>
#include <stdlib.h>

/**
 * @brief Finds the missing number in an array containing numbers from 0 to n.
 *
 * @param nums The input array (missing one number).
 * @param n The size of the input array (meaning the numbers should be from 0 to n).
 * @return int The missing number.
 */
int findMissingNumber(int nums[], int n) {
    // n is the size of the array, so the full set of numbers ranges from 0 to n (n+1 numbers total).
    
    // Calculate the expected sum of all numbers from 0 to n.
    // We use a long long to prevent potential integer overflow, as n*n can be large.
    long long expected_sum = (long long)n * (n + 1) / 2;
    
    // Calculate the actual sum of the elements present in the array.
    long long actual_sum = 0;
    for (int i = 0; i < n; i++) {
        actual_sum += nums[i];
    }
    
    // The difference is the missing number.
    return (int)(expected_sum - actual_sum);
}

int main() {
    // --- Sample Test Cases ---
    
    // Input 1: nums1 = [0,3,2,4]. n=4, numbers range 0-4. Missing 1.
    int nums1[] = {0, 3, 2, 4};
    int n1 = sizeof(nums1) / sizeof(nums1[0]);
    int result1 = findMissingNumber(nums1, n1);
    printf("Input 1: Missing Number: %d (Expected: 1)\n", result1);

    // Input 2: nums1 = [1,2,3]. n=3, numbers range 0-3. Missing 0.
    int nums2[] = {1, 2, 3};
    int n2 = sizeof(nums2) / sizeof(nums2[0]);
    int result2 = findMissingNumber(nums2, n2);
    printf("Input 2: Missing Number: %d (Expected: 0)\n", result2);

    // Input 3: nums1 = [0,4,3,1,5]. n=5, numbers range 0-5. Missing 2.
    int nums3[] = {0, 4, 3, 1, 5};
    int n3 = sizeof(nums3) / sizeof(nums3[0]);
    int result3 = findMissingNumber(nums3, n3);
    printf("Input 3: Missing Number: %d (Expected: 2)\n", result3);
    
    // Input 4: Large array test. n=99, numbers range 0-99. Missing 50.
    // Creating the array manually for a better test case
    int n4 = 100; // Full set is 0 to 100
    int nums4[100]; // Array size is 100, so n=100
    int k = 0;
    for (int i = 0; i <= n4; i++) {
        if (i != 50) { // Skip 50
            nums4[k++] = i;
        }
    }
    // The actual input array size is n4 - 1 = 99
    int result4 = findMissingNumber(nums4, 99); 
    printf("Input 4 (0-99): Missing Number: %d (Expected: 50)\n", result4);

    return 0;
}